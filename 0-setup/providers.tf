provider "azurerm" {
  # subscription_id = "STORED IN ENV VARIABLE ARM_SUBSCRIPTION_ID"  # client_id       = "STORED IN ENV VARIABLE ARM_CLIENT_ID"  # client_secret   = "STORED IN ENV VARIABLE ARM_CLIENT_SECRET"  # tenant_id       = "STORED IN ENV VARIABLE ARM_TENANT_ID"
}
